// TASK C STUB — an orchestrating agent will replace this file's body.
// Contract (see SPEC.md "Widgets / filter.ts" + "DOM contracts"):
//   Wire the .kb-filter <input> to live fuzzy-filter the .kb-row elements
//   of the current app page, using fuzzyScore from ./fuzzy.
// Must keep this exact export signature.

import { fuzzyScore } from "./fuzzy";

export function initFilter(): void {
  void fuzzyScore;
  // TODO(agent C): implement per SPEC.md
}
